final define my_string: string = "Hello, World!"

define my_variable: i32 = 0
my_variable++
info "my_variable is ${my_variable}"

define my_fld_sig: signature(field) = signature <com/example/Foo>.test: i32
define my_fun_sig: signature(fun) = signature <com/example/Foo>.test(): void
define my_insn: instruction = instruction(ldc "Hello, World!")
define my_op: opcode = opcode invokestatic

define my_array: [i32] = []{0, 1, 2, 3}
my_array += 4
my_array -= []{0, 2}

for(value in my_array) {
    info "my_array value is: ${value}"
}

for(i in 0..<sizeof(my_array)) {
    info "my_array value at ${i} is: ${my_array[i]}"
}

inject <com/example/TargetClass>.someFunction(): void by MySelector {
    return
}

inject <com/example/TargetClass>.someField2: string {
    define access: i32 = field.access
}

static sync fun <com/example/TargetClass>.someFunction2(): void {
    define insns: [instruction] = fun.instructions
    define access: i32 = fun.access
    return
}

private static field <com/example/TargetClass>.someField: i32 = 0

type MyAliasedType = i32
type MyAliasedType2 = <com/example/Testing>
define my_type_var: type = type MyAliasedType
define my_type_var2: type = type <com/example/Testing>

^class MyPreproType(x: i32, y: i32, z: i32)